# Hostinger Node.js (Passenger)
# Route ALL requests through Node.js — no exceptions

DirectoryIndex disabled

RewriteEngine On

# Pass every single request to Node.js/Passenger
# No file/directory existence checks — Node.js handles everything
RewriteRule ^(.*)$ /$1 [L,PT]
